/**
 * @module domain/tools
 */
/**
 * Enum which holds all valid tool ids.
 * @enum
 */
export const Tools = Object.freeze({
	MEASURE: 'measure',
	DRAW: 'draw',
	SHARE: 'share',
	IMPORT: 'import',
	EXPORT: 'export'
});
